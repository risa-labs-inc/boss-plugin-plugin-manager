# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# IDE
.idea/
*.iml
*.ipr
*.iws
.project
.classpath
.settings/

# OS
.DS_Store
Thumbs.db

# Local properties
local.properties
gradle.properties
.claude/
.codex/
.gemini/
.opencode/

# Git worktrees. Feature checkouts live under .worktrees/, so without this the directory
# shows up as untracked in `git status` on every branch. Ignored rather than tracked because
# what lives there is per-developer and per-task.
.worktrees/
